apiVersion: v1
kind: Service
metadata:
  name: {{ include "genai-platform.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "genai-platform.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "genai-platform.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
